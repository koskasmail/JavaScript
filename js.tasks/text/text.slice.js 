/* ----[text slice]------------
*  * slice first 5 characters into a variable
*/

let text = "https://github.com/koskasmail";
let result = text.slice(0, 5);
console.log(`text:::: ${text}`);
console.log(`result:::: ${result}`);